[package]
name = "printer-service"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
encoding = "0.2"

[build-dependencies]
tonic-build = "0.12"
